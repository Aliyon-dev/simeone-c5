"use client"

// Smooth scrolling is now handled by CSS (scroll-behavior: smooth) in globals.css
// This component is kept for potential future enhancements
export function SmoothScroll() {
  return null
}
